import {Header,Footer} from '../components';import {researchPosts,site} from '../data';export const metadata={title:`Research | ${site.brand}`,description:'Research notes for Philippines-based staffing decisions.'};export default function Research(){return <><Header/><main className="section"><div className="container"><p className="eyebrow">Philippines staffing research</p><h1>Research for better role decisions.</h1><p className="lead">This library will publish sourced research about Philippines-based staffing and operations. No research articles are published yet.</p>{researchPosts.length?<div className="cards">{researchPosts.map(p=><a className="card" href={`/research/${p.slug}`} key={p.slug}><h2>{p.title}</h2><p>{p.excerpt}</p></a>)}</div>:<div className="support-strip"><h2>Research library in preparation</h2><p>Return for Philippines-focused research notes as they are published.</p></div>}</div></main><Footer/></>}
