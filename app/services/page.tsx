import {Header,Footer,CTA} from '../components'; import {services,site} from '../data';
export const metadata={title:`Services | ${site.brand}`,description:`Philippines-based ${site.primary} services with clear task scope and review.`};
export default function Services(){return <><Header/><main><section className="architecture-hero"><div className="container"><p className="eyebrow">Philippines service directory</p><h1>Specialized support for a defined workload.</h1><p className="lead">Choose a Filipino support lane built around repeatable tasks and written controls. Each service page explains the role before the staffing intake begins.</p></div></section><section className="section"><div className="container service-directory">{services.map(s=><a className="card" href={`/services/${s.slug}`} key={s.slug}><h2>{s.title}</h2><p>{s.desc}</p><span>Explore service →</span></a>)}</div></section><CTA/></main><Footer/></>}
